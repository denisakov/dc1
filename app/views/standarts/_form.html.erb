<%= form_for(@standart) do |f| %>
  <% if @standart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@standart.errors.count, "error") %> prohibited this standart from being saved:</h2>

      <ul>
      <% @standart.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br />
    <%= f.text_field :project_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
