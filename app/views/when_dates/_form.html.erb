<%= form_for(@when_date) do |f| %>
  <% if @when_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@when_date.errors.count, "error") %> prohibited this when_date from being saved:</h2>

      <ul>
      <% @when_date.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.datetime_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
