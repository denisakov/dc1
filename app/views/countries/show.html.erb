<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @country.name %>
</p>

<p>
  <b>Project:</b>
  <% @country.projects.each do |p| %>
	  <%= link_to p.refno, project_path(p) %>
  <% end %>
</p>


<%= link_to 'Edit', edit_country_path(@country) %> |
<%= link_to 'Back', countries_path %>
